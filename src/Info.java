
public interface Info {
	// This is an interface
	/* An interface doesn't have any code in it, so we don't use the ' { } ' here 
	 * They only contain a header of methods
	 */
	
	public void showInfo();
}
